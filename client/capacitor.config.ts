import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.restful.home',
  appName: 'Restful Home',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
